# CPUSchedulingSimulator
This project demonstrates three classic CPU scheduling algorithms and computes performance metrics for each. It was written from scratch in C using separate compilation and a custom Makefile.
